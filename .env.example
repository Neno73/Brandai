# BrendAI Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# Database (Neon Postgres via Vercel)
# Get from: vercel env pull .env.local OR neonctl connection-string
DATABASE_URL=postgres://user:password@host/database

# External API Keys
# Brandfetch: https://brandfetch.com (Dashboard → API Keys)
BRANDFETCH_API_KEY=your_brandfetch_api_key_here

# Firecrawl: https://firecrawl.dev (Settings → API Keys)
FIRECRAWL_API_KEY=your_firecrawl_api_key_here

# Google Gemini: https://ai.google.dev/tutorials/setup
# Used for: Gemini 2.5 Flash (text generation) and Nanobana (image generation)
GEMINI_API_KEY=your_gemini_api_key_here

# Resend: https://resend.com (API Keys)
RESEND_API_KEY=your_resend_api_key_here

# Vercel Blob Storage: Vercel Dashboard → Storage → Create Store → Blob
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# Authentication Secrets
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MAGIC_LINK_SECRET=generate_random_256_bit_hex_string

# Admin Credentials
ADMIN_EMAIL=admin@example.com
# Generate with: node -e "const bcrypt = require('bcryptjs'); console.log(bcrypt.hashSync('your_password', 10))"
ADMIN_PASSWORD_HASH=generate_bcrypt_hash_of_your_password

# App Configuration
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NODE_ENV=development
